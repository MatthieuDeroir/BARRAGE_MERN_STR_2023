import React from 'react';

export default function BroadcastButton() {
    // TODO: Implement this
    // the broadcast button should send a request to the backend to broadcast the current configuration
    return (
        <div>
            <button>Broadcast</button>
        </div>
    );
}

