const mongoose = require('mongoose');
const DisplaySettings = require('../Models/displaySettingsModel');

async function initializeDatabase(DisplaySettingsModel) {
    // Your initialization logic here
}

module.exports = {
  initializeDatabase,
};
